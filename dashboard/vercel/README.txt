# MulT Ops Console — Vercel static export
#
# Preferred Vercel Root Directory: dashboard/web
#   (Vercel runs `npm run build` → serves `out/`)
#
# Fallback Root Directory: dashboard/vercel
#   (prebuilt static files synced from dashboard/web/out)
#
# Do NOT leave Root Directory empty (repo root) — that causes 404 NOT_FOUND.
